flightrecorder:
  enabled: true
  trigger:
    - expression: meter('jvm.memory.used').tag('area','nonheap').tag('id','Metaspace').value('value') > 100
      startRecordingCommand:
        duration: 60
        timeUnit: SECONDS